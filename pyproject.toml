[project]
name = "portfolio"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6",
    "jinja2>=3.1",
    "pydantic>=2.6",
    "markupsafe",
]

[tool.pytest.ini_options]
testpaths = ["cv/tests"]

[dependency-groups]
dev = [
    "pytest>=8",
]
