.venv/
__pycache__/
*.pyc
*.swp
*.swo
.pytest_cache/

# CV LaTeX artifacts (regenerated)
cv/generated/
cv/main.pdf
cv/main.aux
cv/main.log
cv/main.out
cv/main.fdb_latexmk
cv/main.fls
cv/main.synctex.gz

# Website artifacts (regenerated). website/*.html covers index.html and every
# generated standalone page (bio.html, twitter.html, ...). Sources are the
# .j2 templates and website/pages/*.md, which are tracked.
website/*.html
website/headshot.jpg
website/CNAME
# Real attention-overlay data (generated per-account from Umami). The committed
# website/heatmap-data.sample.json is the template/demo the tool falls back to;
# generate the real one with scripts/umami-heatmap-export.py (local only).
website/heatmap-data.json
# Generated arXiv sources for garage posts (main.tex + copied figures + LaTeX
# build junk). The SOURCE is website/pages/garage-*.md; committed fallback
# figures live in website/img/garage/<slug>/ (tracked).
website/garage/

# Private working notes (application targets, honors collation) — intentionally
# uncommitted; see notes/applications-and-honors.md
notes/

# Overleaf CV publish staging — a local mirror (CV source + generated tex, no
# website) pushed to the Overleaf git remote. Not part of the portfolio repo.
.overleaf-cv/

# Memory & harness
.claude-memory/
.xdg-config/
